<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nova.agent.repository.ConversationMapper">

    <resultMap id="BaseResultMap" type="com.nova.agent.model.po.Conversation">
        <id column="id" property="id"/>
        <result column="app_id" property="appId"/>
        <result column="conversation_id" property="conversationId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.nova.agent.model.po.Conversation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO conversation (app_id, conversation_id, create_time)
        VALUES (#{appId}, #{conversationId}, COALESCE(#{createTime}, NOW()))
    </insert>

    <select id="selectByConversationId" resultMap="BaseResultMap">
        SELECT * FROM conversation WHERE app_id = #{appId} AND conversation_id = #{conversationId} LIMIT 1
    </select>

</mapper>
